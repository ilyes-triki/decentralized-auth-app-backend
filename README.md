# Decentralized Auth — Backend

This repository folder contains the **Spring Boot** API.

Documentation, prerequisites, configuration, and run instructions are maintained in:

**[`backend/README.md`](backend/README.md)**

```bash
cd backend
./mvnw spring-boot:run
```
